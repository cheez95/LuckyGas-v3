# Security-specific requirements for LuckyGas backend
# These packages should be kept up to date for security

# Core security
cryptography>=42.0.0  # Latest for security fixes
bcrypt>=4.3.0  # Password hashing
passlib>=1.7.4  # Password utilities
python-jose[cryptography]>=3.5.0  # JWT handling

# 2FA and authentication
pyotp>=2.9.0  # TOTP/HOTP for 2FA
qrcode>=7.4.2  # QR code generation
pillow>=10.2.0  # Image processing (for QR codes)

# Security scanning and validation
email-validator>=2.2.0  # Email validation
# bandit>=1.7.5  # Security linting (dev dependency)
# safety>=3.0.0  # Dependency vulnerability scanning (dev dependency)

# Rate limiting and caching
redis>=6.2.0  # For rate limiting and session storage

# SSL/TLS
certifi>=2023.0.0  # Updated CA certificates

# Monitoring and logging
python-json-logger>=2.0.7  # Structured logging
prometheus-fastapi-instrumentator>=6.1.0  # Metrics

# Keep these core dependencies updated
fastapi>=0.116.1  # Web framework
pydantic>=2.11.7  # Data validation
sqlalchemy>=2.0.41  # ORM
uvicorn>=0.35.0  # ASGI server
httpx>=0.28.1  # HTTP client

# Database drivers with security updates
asyncpg>=0.30.0  # PostgreSQL async driver
psycopg2-binary>=2.9.10  # PostgreSQL driver

# Google Cloud security
google-cloud-secret-manager>=2.16.0  # Secrets management
google-cloud-storage>=2.10.0  # Secure storage

# Testing security
pytest>=8.4.1
pytest-asyncio>=1.1.0
faker>=22.0.0  # For generating test data